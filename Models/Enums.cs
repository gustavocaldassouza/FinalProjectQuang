using System;

namespace FinalProjectQuang.Models
{
    public enum UserRole
    {
        Owner,
        Manager,
        Tenant
    }

    public enum ApartmentStatus
    {
        Available,
        Rented,
        UnderMaintenance
    }
}
